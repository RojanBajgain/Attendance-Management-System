import 'package:flutter/material.dart';

class TaxDetailsView extends StatelessWidget {
  const TaxDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tax Details'),
    );
  }
}
